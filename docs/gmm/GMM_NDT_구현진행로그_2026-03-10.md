# GMM_NDT 구현 진행 로그 (2026-03-10)

## 작업 목표

- `IntegratedGMMNDTFactor` 파일 세트 추가
- `gtsam_points` 빌드 연결
- `src/main.cpp`에 `GMM_NDT` factor 등록
- 빌드 및 headless smoke 검증

## 진행 상태

### Step 1. 초기화

- 상태: 완료
- 작업 내용:
  - 기존 `IntegratedNDTFactor`/`IntegratedLightNDTFactor` 패턴 점검 완료
  - 신규 factor 파일 세트 구조 확정

### Step 2. factor 파일 생성

- 상태: 완료
- 작업 내용:
  - `integrated_gmm_ndt_factor.hpp` 생성
  - `impl/integrated_gmm_ndt_factor_impl.hpp` 생성
  - `src/gtsam_points/factors/integrated_gmm_ndt_factor.cpp` 생성
  - 핵심 로직: top-K 후보 선택 + responsibility(gamma) 계산 + 가중 Hessian 누적

### Step 3. 빌드/엔트리 연결

- 상태: 완료
- 작업 내용:
  - `thirdparty/gtsam_points/CMakeLists.txt` 소스 등록 반영
  - `src/main.cpp`에 `GMM_NDT` factor 분기 연결 반영
  - `src/main.cpp` factor list에 `GMM_NDT` 항목 추가

### Step 4. 빌드/실행 검증

- 상태: 완료
- 작업 내용:
  - Docker 빌드 성공: `cmake --build . -j8`
  - 단일 팩터 smoke 성공: `--headless --factor GMM_NDT --threads 8`
  - 전체 headless 실행 성공: `--headless --threads 8`
  - 최종 비교표에 `GMM_NDT` 행 출력 확인

### Step 5. 1차 수치 확인

- 상태: 완료
- 결과 요약(threads=8, full headless 1회):
  - `GMM_NDT`: Mean T `0.099698`, Mean R `0.771133`, Max T `0.174744`, Max R `1.370901`, `9791 ms`
  - `NDT`: Mean T `0.105657`, Mean R `0.730808`, Max T `0.191520`, Max R `1.320227`, `5970 ms`
- 메모:
  - 현재 구현은 `num_components=2`, `gamma_min=1e-4`, `freeze_mixture_in_lm=false` 기본값으로 연결됨
  - 속도는 NDT 대비 느리며, 추가 튜닝/안정화가 필요

## 체크리스트

- [x] `integrated_gmm_ndt_factor.hpp` 생성
- [x] `impl/integrated_gmm_ndt_factor_impl.hpp` 생성
- [x] `src/gtsam_points/factors/integrated_gmm_ndt_factor.cpp` 생성
- [x] `thirdparty/gtsam_points/CMakeLists.txt` 등록
- [x] `src/main.cpp` factor list / create_factor 분기 추가
- [x] Docker 빌드/실행 검증
